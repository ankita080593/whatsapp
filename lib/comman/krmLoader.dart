import 'package:flutter/material.dart';
class krmLoader extends StatelessWidget {
  const krmLoader({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: CircularProgressIndicator(),),

    );
  }
}
